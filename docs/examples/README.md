# Example Data

These files provide shared sample product data for the repository:

- `sample-goals.json`: representative user goals
- `sample-users.json`: simple starter users
- `example-training-plan.json`: a believable adaptive week

They are intended to keep the docs, web app, iOS app, and backend aligned around the same product concepts.
